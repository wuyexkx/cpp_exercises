#include <iostream>
#include <list>
using namespace std;


/*
auto 自动推断变量类型, 根据返回值自动推断
list<string>::iterator ite;
ite = find(c.brgin(), c.end(), target);
        |
        v
auto ite = find(c.brgin(), c.end(), target);
*/

int main()
{
    return 0;
}
